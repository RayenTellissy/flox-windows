import { ScreenPlaceholder } from "placeholder.slint";

export component LibraryScreen inherits ScreenPlaceholder {
    eyebrow: "LIBRARY";
    heading: "Library";
    caption: "Everything uploaded to the channel.";
}
