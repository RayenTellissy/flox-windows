import { ScreenPlaceholder } from "placeholder.slint";

export component SettingsScreen inherits ScreenPlaceholder {
    eyebrow: "SETTINGS";
    heading: "Settings";
    caption: "Playback, audio, interface, library, account and tools.";
}
