import { Tokens } from "../tokens.slint";
import { ScreenPlaceholder } from "placeholder.slint";

export component PlayerScreen inherits ScreenPlaceholder {
    background: Tokens.pure-black;
    eyebrow: "PLAYER";
    heading: "Player";
    caption: "Video plays under the overlay.";
}
