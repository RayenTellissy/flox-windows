import { ScreenPlaceholder } from "placeholder.slint";

export component LoginScreen inherits ScreenPlaceholder {
    eyebrow: "TELEGRAM";
    heading: "Connect Telegram";
    caption: "Scan with the Telegram app, or use a phone number.";
}
